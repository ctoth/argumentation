**You are a WORKER agent launched via the Task tool. Execute this task directly. Do NOT read foreman.md. Do NOT coordinate — DO the work yourself.**

# Task: Locate the ICCMA 2023 benchmark path and operational bottleneck surfaces

Read the full researcher skill at `C:/Users/Q/.codex/plugins/cache/protocols-marketplace/protocols/0.1.3/skills/researcher/SKILL.md`, run its required activation, and inspect the current repository at `C:/Users/Q/code/argumentation`. The parallel frontier and native-runner work is already merged into local `main` at merge commit `84cbb04`.

Produce one evidence-backed report at `docs/reports/iccma2023-repo-research-2026-07-11.md` answering: what exact local ICCMA 2023 corpus/assets exist; what exact current command should establish a bounded baseline; which tasks/families should be run first to find slowness; and what existing deterministic telemetry, calibrated contracts, profiler hooks, and worker-process boundaries can satisfy the repository performance-contract-before-research rule. Cite file paths and line numbers and distinguish verified facts from unknowns. Do not run a long benchmark, implement, edit source, or modify anything except the report.

Do not speculate. Every claim in your report must be verified by reading source code or observing command output. If you cannot verify something, say "I did not verify this". Trace the actual code path.

Never write `python -c` or `uv run python -c`. Write a reusable script file if substantive logic is required, but this task should remain read-only apart from its report.

You are running alongside other workers. Never use `git stash`, `git restore`, `git checkout`, `git reset`, or `git clean`. Do not commit.
