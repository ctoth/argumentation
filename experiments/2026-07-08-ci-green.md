# CI green (exp/ci-green)

## Diagnosis reference

`reports/ci-red-investigation.md` (main tree): three failures on `main`
(last green `5fc78e3` 2026-06-28, first red `7bc7fb7` 2026-06-29), fully
classified — one campaign-introduced stale test (`bc140ba`), one docs-only
regression (`db73c8b`), one pre-existing PYTHONHASHSEED tie-break flake.

## Fixes (one commit each, on exp/ci-green from main@cc50c4a)

1. **2cd96ac** — `tests/test_performance_contracts.py`: the stable-route
   contract still pinned the pre-`bc140ba` dispatch (151-assumption
   large-dense fixture -> SAT). Renamed to
   `test_large_dense_stable_auto_route_uses_asp_when_not_sparse_narrow`
   (full-solve: forbids `sat_aba_stable_extension`, mocks
   `_solve_asp_aba_single_extension`) and added
   `test_sparse_narrow_stable_auto_route_uses_sat_without_asp` with a
   compliant sparse-narrow fixture (700 assumptions, width-1 rules, distinct
   contraries) asserting `_auto_aba_backend_for_framework(...) == "sat"`,
   mirroring the sibling update in `tests/solving/test_solver_availability.py`.
   No src change — the routing gate is intended and evidence-backed.
2. **96e23ed** — `docs/argumentation-package-boundary.md`: the relocated
   propstore doc cited the pre-decomposition flat layout; five entries tripped
   `test_current_docs_do_not_cite_old_flat_source_paths` (af_revision, dung,
   preference, probabilistic, probabilistic_treedecomp — the prompt's "one
   offender" was the truncated CI excerpt). Refreshed the kernel-module and
   source-file lists to current package paths, including the treedecomp split
   (`probabilistic_treedecomp_construction` / `probabilistic_paper_td` /
   `probabilistic_grounded_td`). Test not weakened.
3. **0bfb172** — `tools/collapsed_profile_summary.py`: `Counter.most_common`
   tie-break follows insertion order; the inclusive counter is built from
   `set(frames)` iteration, so equal-share rows ordered per PYTHONHASHSEED.
   Added `top_frames` ranking by `(-samples, frame)` (source fix, not test).
   Added `scripts/run_hashseed_matrix.py` prover.

## Evidence

Hashseed matrix, `tests/test_collapsed_profile_summary.py` (fresh interpreter
per seed):

| Seed | Pre-fix | Post-fix |
|---|---|---|
| 0 | FAIL | PASS |
| 1 | FAIL | PASS |
| 2 | FAIL | PASS |
| 3 | FAIL | PASS |
| 4 | FAIL | PASS |
| 5 | PASS | PASS |
| 6 | FAIL | PASS |
| 7 | PASS | PASS |
| 8 | FAIL | PASS |
| 9 | PASS | PASS |

Pre-fix: 3/10 seeds passed (failing: 0,1,2,3,4,6,8) — RED evidence matches
the known 7/10-fail debt. Post-fix: 10/10.

CI-equivalent gates on the final tree:

- `uv run pytest -q --timeout=600`: fully green (see reports/ci-green-coder.md
  for the tail).
- `uv run pyright src`: 0 errors, 0 warnings. Also 0/0 on touched
  tools/scripts/tests files.
- `uv run lint-imports`: Contracts: 2 kept, 0 broken.
- `uv build`: built sdist + wheel 0.3.0.

## Decision

Recommend merging exp/ci-green to main (recommend-only; no push/merge
performed). The three fixes are exactly the diagnosed failures; no skip
markers, no deselection, no test weakened or deleted.
